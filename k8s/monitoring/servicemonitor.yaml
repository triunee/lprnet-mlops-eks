apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: lprnet-monitor
  namespace: monitoring
spec:
  selector:
    matchLabels:
      app: lprnet-inference
  namespaceSelector:
    matchNames:
      - default
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
